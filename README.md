# Aureus Wealth Terminal

Aureus is a local-first, visualization-first Personal Wealth Intelligence Terminal for macOS.

The product is designed around CNY and USD, with CNY as the default unified valuation currency. It keeps original-currency values, exchange-rate context, and converted values distinct. AI and LLM capabilities are outside the product's long-term scope.

Planned product areas are:

- Dashboard
- Wealth
- Markets
- Portfolio
- Analytics
- Ledger
- Goals
- Settings

Privacy is a hard boundary: real accounts, balances, holdings, transactions, databases, backups, credentials, and private imports or exports must not enter the future public repository. Demos and tests may use only synthetic or sanitized data.

## Current Status

当前最新状态：**Stage 11-APP-CONNECTION-DIAGNOSTIC-CLOSURE-02 PARTIAL — Awaiting Reviewer Gate**。14 项指定 Mandatory Read 正文由主 Executor 按序实际审阅至 EOF，其余冻结约束通过治理更新后的 116 项安全路径/Hash 基线继承。源码、四项 signed 产品身份及启动前相关进程检查通过；没有修改源码、build 或 re-sign。

Gate A 唯一 invocation 的 underlying `xcodebuild` 退出码已即时保存为 `64`：当前 Xcode 拒绝本 Prompt 指定的 `-test-iterations 1`，报错 `Must specify -test-iterations with more than 1 iteration.`。实际 bundle 含 `Info.plist`，summary/tests parsers `0/0`，canonical result `unknown`，definitions/executions `0/0`，outcomes `0/0/0`，没有进入 Ledger 业务。它是命令参数校验失败，不是业务 PASS，也不是历史断连复现。按零 retry 预算停止，Existing focused 和 Full UI 均 `NOT RUN`；未改参数重跑。历史首次断连原因仍 `UNKNOWN`。

完整本轮记录：[ExecutionReport.md](/private/tmp/Aureus-Stage11-APP-CONNECTION-DIAGNOSTIC-CLOSURE-02-KNimB7/ExecutionReport.md)。测试结束、文档修改前 116 项与产品 Hash 不变；仅本节及 Stage 11 acceptance 新增当前证据。历史失败、阅读缺口与 numeric exit `NOT VERIFIED` 保留。Provider requests `NOT RUN`，persistent writes `Disabled`，retention rights `BLOCKED`，Stages 12–14 `NO-GO`。没有运行 synthetic 业务 fixture；未观测的全进程 live-operation 计数不伪报为遥测零值。

以下为前轮启动前停止的原始记录，保留历史：

当前最新状态：**Stage 11-APP-CONNECTION-DIAGNOSTIC-CLOSURE-01 PARTIAL — Awaiting Reviewer Gate**。本轮 32 项 Mandatory Read 正文已按序实际审阅至 EOF；基线清单自身 Hash、116 项源码和四项冻结产品 Hash 全匹配。启动前发现 PID `62705` 来自旧 `BACKUP-FOUNDATION` unsigned Aureus 产品，其当前运行用途/归属未确认，按停止规则未启动任何测试，也未终止该进程。Ledger diagnostic、Existing focused `12/12`、Full UI `18/18` 均为 `NOT RUN`。本轮零源码修改、零 build/sign、零 retry。

历史 Targeted `1/1 PASS` 仅在精确源码/产品核验后继承；历史 Existing `0/12/0 FAIL`、历史 Mandatory Read 未满足及数字退出码 `NOT VERIFIED` 保持不变。首次断连原因仍为 `UNKNOWN`，本轮没有执行，不能声称“未复现”或“已修复”。完整报告：[ExecutionReport.md](/private/tmp/Aureus-Stage11-APP-CONNECTION-DIAGNOSTIC-CLOSURE-01-OMbBhJ/ExecutionReport.md)。仅更新两份文档；Provider requests `NOT RUN`，Twelve Data persistent writes `Disabled`，Provider retention rights `BLOCKED`，Stages 12–14 `NO-GO`。

以下为上一轮原始结论，保留历史，不追溯改写：

当前状态：**Stage 11-EXTERNAL-RESTORE-DIRECTORY-PANEL-CLOSURE-01 PARTIAL — Awaiting Reviewer Gate**。本轮只修改 UI Test 的 `chooseDirectory`：保留 Go To Folder 流程，以一次公开 XCTest `.enter` 提交目录默认动作。当前 Targeted External Restore UI 已取得完整 canonical `1/1 PASS`，覆盖两种 Cancel、真实 Export/Restore、两文件 artifact、safety generation、Goals reconstruction 与隔离模式重启。随后同一冻结产品的 Existing focused 形成完整 `12/12` executions、`0/12/0 FAIL`：Ledger 在执行期间失去应用连接，其余测试随后在公共 pre-launch helper 中发生相同连接错误；Full UI `18/18` 为 `NOT RUN`。本轮没有正式 Gate 后 repair、business retry、infrastructure retry 或 incomplete-result re-observation。Reviewer 仍独立决定 Stage 11 Gate。

以下保留截至前一轮的历史状态；本轮实际结果与证据限制见文末及 [Stage 11 acceptance](docs/STAGE11_DATA_LIFECYCLE_ACCEPTANCE.md)。

Stages 1 through 10 and the Stage 11 Backup, Restore, and Migration Safety foundations have passed their applicable Reviewer gates. Prompt 11-EXISTING-UI-DIAGNOSTIC-CLOSURE-02 made no Product, Test, Project, Package, Migration, Entitlement, Target, Scheme, fixture, build, or signing change. Exact source and frozen-product verification accepted the current-source External Export targeted UI `1/1 PASS`, while the preceding unfinalized Existing focused directory remains **INCOMPLETE RESULT — NOT PASS / NOT VERIFIED** and was neither repaired, merged, nor converted into canonical counts. On the same frozen signed product, Ledger Dynamic, Native CSV, and Portfolio each produced an independent complete `1/1 PASS`; the ordered single-invocation Existing focused regression then produced canonical `11/11 PASS`, and the single-invocation full `AureusUITests` produced canonical `17/17 PASS`. Prompt 11-EXTERNAL-BACKUP-RESTORE-VALIDATION-CLOSURE-01 then corrected the schema-0 untrusted-manifest fixture and removed standalone External generation validation's parent-directory access dependency without changing Internal Backup/Restore direct-child enforcement. Prompt 11-SETTINGS-EXTERNAL-BACKUP-RESTORE-UI-01 wires that accepted Foundation into Settings with an operation-scoped folder importer, security-scoped lease, explicit confirmation sheet, finite model state, and current-source Unit/build coverage. Its final Focused Unit passed `134` definitions / `168` dynamic executions, Affected Regression passed `167/205`, Full Unit passed `460/531`, and Clean Debug arm64 Build plus fresh signed BFT passed with no new warnings. The first Targeted UI run exposed two competing folder-importer presentations; the single authorized repair consolidated them into one shared importer. The final, second Targeted business execution then reached the real External Export folder panel but failed at `AureusUITests.swift:3042` because the current panel did not expose the public Choose control after Go To Folder. That complete `0/1/0` failure exhausted the business-execution and repair budget, so Existing focused `12/12` and full UI `18/18` are `NOT RUN`. The current execution state is **Stage 11-SETTINGS-EXTERNAL-BACKUP-RESTORE-UI-01 PARTIAL — Awaiting Reviewer Gate**. Provider requests remain `NOT RUN`, Twelve Data persistent writes remain `Disabled`, Provider retention rights remain `BLOCKED`, and Stages 12–14 remain `NO-GO`.

The preceding **Stage 10 Goals UI Runtime Candidate — Awaiting Reviewer Gate** remains accepted historical evidence. Prompt 10-GOALS-UI-GATE-CLOSURE-01 changed no Production Swift, Test Swift, Project, Package, Migration, Entitlement, Target, Scheme, fixture, or Unit test. Exact source and signed-product verification accepted the current-source Goals focused UI `1/1 PASS`, then the same frozen App, Runner, UI Test executable, and xctestrun produced a complete Existing focused regression `8/8 PASS` and a complete full `AureusUITests` `14/14 PASS`; both current result bundles contain `Info.plist` and passed canonical summary/tests parsing. Its prior incomplete Existing focused bundle remains preserved as `INCOMPLETE RESULT — NOT PASS / NOT VERIFIED`; it was not repaired, combined, or substituted. That closure used no infrastructure retry, incomplete-result re-observation, or business retry.

The preceding **Stage 10-GOALS-CHART-AX-01 PARTIAL — Awaiting Reviewer Gate** remains historical evidence. The real Trajectory and Saving Rate Swift Chart visual groups expose independent, self-contained native Accessibility elements with the exact labels `Goal trajectory chart` and `Observed Saving Rate chart`; their summaries, tables, rows, disclosures, data, and USD typed-unavailable behavior remain separate and unchanged. Its Clean Debug arm64 Build and fresh signed build-for-testing passed. The initial Goals focused invocation was a zero-business Automation bootstrap failure; the single same-product infrastructure retry entered the business method and formally passed both chart count/exact-label contracts and their summary/table/row/disclosure assertions, then exposed the later `goals.evidence` label failure at `AureusUITests.swift:1819`. That downstream Calculation Evidence boundary was outside the chart-only authorization, so no business retry was performed; Existing focused `8/8` and full UI `14/14` were `NOT RUN` in that round.

The preceding **Stage 10-GOALS-HEADER-AX-01 PARTIAL — Awaiting Reviewer Gate** remains historical evidence: it made the page, current mode, and local-only disclosure independent visible native Accessibility siblings. Its same-product infrastructure retry passed the Synthetic Header and Goal CRUD before stopping at the then-unlabeled Trajectory chart; that failure is preserved in the Stage 10 acceptance history and is not represented as a current UI PASS.

The preceding **Stage 10-GOALS-UI-01 PARTIAL — Awaiting Reviewer Gate** result remains historical evidence: its two current-source Goals focused business executions each passed `goals.page` and stopped because `goals.mode.synthetic` was not independently queryable. The accepted foundation still establishes validated Goal domain values, permanent CRUD through the schema-version-6 `goals` table, deterministic checked-Decimal planning scenarios, Goal/FIRE progress, Ledger-derived Saving Rate analysis, and an explicit-calculate session FeatureModel. The Native Goals Terminal adds a Goals destination, synthetic-only Goal seeding, CRUD, session inputs, five finite report sections, Swift Charts presentation, and focused UI coverage without Provider, Credential, Keychain, or planning-assumption persistence access. Portfolio remains a local-first terminal with Portfolio CRUD, Wealth security links, Opening Lot/Buy/Sell/Manual Split activities, deterministic FIFO lots, checked-Decimal CNY cost basis and P&L, quantity reconciliation, atomic Portfolio NAV snapshots, allocation, a native P&L heatmap, and an identifier-only session Benchmark boundary.

Stage 8A separates the immutable local-record policy from dynamic Benchmark state. `portfolio.disclosure` always exposes “No Provider request” through its own native Accessibility node, while `portfolio.benchmark.disclosure` reports only the current session Benchmark state. The holding-summary path now reads links, activities, and relevant Wealth records once, replays FIFO once, groups lots and realized results once, and computes each holding from its own group before applying Portfolio NAV weights. Release measurements cover full 10,000-activity replays and appended Buy/Sell replays, 100-holding summaries, 5,000-snapshot preparation, and 100 Portfolio switches; an exact Synthetic Demo Portfolio idle sample remained at 0.0% CPU and 74 MiB across six one-second samples.

Stage 8AC removes the confirmed-delete TOCTOU race by synchronously capturing the alert's immutable Portfolio UUID and passing that identifier into the asynchronous delete command; dismissal and later selection changes can no longer erase or retarget an already confirmed command. Four deterministic lifecycle tests cover dismissal, cancellation, selection change, and Store-failure isolation. The expanded Stage 8 focused suite passed 21/21 definitions, Stage 6/7 regression passed 107 definitions (116 parameterized executions), full Unit/Integration passed 231 definitions (264 parameterized executions), and final clean build plus build-for-testing passed. Portfolio focused UI then passed its complete create/reorder/reload/single-confirm-delete/second-reload/Production-isolation flow, and Ledger focused UI passed without a retry. The single authorized Markets business execution completed Synthetic Search, chart/accessibility, Watchlist and Session Clear, but the executor turn was interrupted during its Production-isolation tail; the result bundle was not finalized, so Markets is `NOT VERIFIED` for this round and was not rerun. Existing focused regression and full UI are therefore `NOT RUN` under the Gate prerequisites. Provider requests were `NOT RUN`, Twelve Data persistent writes remain `Disabled`, retention remains `BLOCKED`, and Stage 9 remains `NO-GO` pending independent Reviewer action.

Stage 8ACA changed no Production or Test Swift source. Exact Stage 8AC source-hash verification preceded a new signed UI build-for-testing. The single authorized Markets focused execution passed `1/1`, the single Existing focused regression passed all `6/6` selected tests, and the single full `AureusUITests` execution passed `12/12`; every result bundle finalized with `Info.plist` and was parsed successfully. These synthetic/local UI results complete the evidence requested by Prompt 8ACA but do not decide the Reviewer Gate or expand live Provider capability. Provider requests remain `NOT RUN`, Twelve Data persistent writes remain `Disabled`, retention remains `BLOCKED`, and Stage 9 remains `NO-GO`.

The Reviewer subsequently recorded the Stage 8 Gate as `PASS` and authorized Stage 9 implementation. Stage 9 replaces the Analytics placeholder with a native, explicit-calculate Portfolio Analytics terminal. It derives cash-flow-adjusted total return, TWR, CAGR, bounded daily-rate XIRR, annualized sample volatility, Sharpe ratio, maximum drawdown, observed monthly/annual TWR, a base-100 wealth index, and drawdown series only from local Portfolio activities and complete Portfolio NAV snapshots. Financial authority remains checked `Decimal`; no metric is persisted and opening Analytics never starts a Provider request. Final focused Analytics Unit/Integration passed `20/20`, Stage 6–8 regression passed `204` definitions (`237` executions), full Unit/Integration passed `254` definitions (`287` executions), the three full-size performance workloads passed, and final clean build plus signed build-for-testing passed. Stage 9 focused UI was attempted once plus the one authorized infrastructure retry, but both UI runners timed out while enabling automation before the business test entered; Stage 9 UI is therefore `NOT VERIFIED`, and the gated existing/full UI suites are `NOT RUN`. This is candidate evidence only, not a Stage 9 Gate decision.

Stage 9A used exact source-hash verification and a fresh signed UI product to replace the earlier bootstrap-only evidence with current-source business executions. The first focused execution entered the complete Synthetic Analytics flow through both charts, then failed because the native `analytics.performance.table` Accessibility surface was not queryable. The single authorized AX-only repair made the seven native table containers explicit accessibility containers without changing calculations, models, persistence, Provider routing, or test assertions. Required post-change verification passed: Stage 9 focused Unit `20/20`, Stage 6–8 regression `204` definitions / `237` executions, full Unit `254` definitions / `287` executions, Clean Debug arm64 Build, and signed build-for-testing. The one authorized final focused business retry nevertheless failed at the same `analytics.performance.table` contract. The focused Gate is therefore `FAIL`; Existing focused UI `7/7` and full UI `13/13` are `NOT RUN` by the ordered Gate. Provider requests remained `NOT RUN`, Twelve Data persistent writes remain `Disabled`, and retention remains `BLOCKED`.

Stage 9AA replaces the unstable parent-container identifiers with seven visible, standalone native summary nodes. Each node has a unique identifier, a self-contained table-purpose label, and the actual row count; performance, drawdown, monthly, annual, subperiod, XIRR-flow, and capital-flow rows remain individually accessible. The strengthened UI test checks those summaries and representative rows without AXValue, fixed coordinates, or row indexes. Required verification passed: Stage 9 focused Unit `20/20`, Stage 6–8 regression `204` definitions / `237` executions, full Unit `254` definitions / `287` executions, Clean Debug arm64 Build, and signed build-for-testing. The first focused business execution failed when an existing chart-summary assertion was outside the visible scroll region. After the one authorized direct lifecycle repair, the final focused retry advanced through metrics, charts, and risk-free disclosure but failed at the existing `analytics.coverage` visibility assertion before reaching the table assertions. The focused result is therefore `FAIL`; Existing focused UI `7/7` and full UI `13/13` are `NOT RUN` under the ordered Gate. Provider requests remained `NOT RUN`, Twelve Data persistent writes remain `Disabled`, retention remains `BLOCKED`, and Stage 10 remains `NO-GO`.

Stage 9AAA adds a bounded, bidirectional, semantic viewport helper to the Stage 9 UI test. It re-queries elements after every `analytics.detail.scroll` swipe and every calculation, Portfolio switch, or relaunch; Production Swift remains unchanged. The initial current-candidate business execution proved `analytics.coverage` was uniquely queryable but found that the test expected `incomplete excluded` while the frozen self-contained AX label says `incomplete snapshots excluded`. The one authorized test-only repair aligned that assertion with the more specific existing label. A fresh final build-for-testing passed, but the only authorized final focused retry exhausted its 8-second bound after three semantic downward swipes and failed before reaching `analytics.chart.performance`. The focused result remains `FAIL`; Existing focused UI `7/7` and full UI `13/13` are `NOT RUN` by the ordered Gate. Unit, regression, performance, and Clean Build evidence is inherited only after exact Production/Unit source-hash verification. Provider requests remain `NOT RUN`, Twelve Data persistent writes remain `Disabled`, retention remains `BLOCKED`, and Stage 10 remains `NO-GO`.

Stage 9AAAA makes the declared scroll-count limit executable: the Stage 9 UI helper now performs a fresh target and scroll-surface query before each bounded traversal step, records actual versus maximum scroll counts without exporting the AX hierarchy, and derives any wall-clock allowance from the finite count (`12 × 4 + 5 = 53` seconds) instead of allowing the former 8-second deadline to terminate traversal after three swipes. The first current business execution completed all top coverage and metric assertions and then performed all 12 semantic scroll attempts before failing at `analytics.chart.performance`. The one authorized test-lifecycle repair changed only the UI test to use element-scoped native relative scrolling; a fresh signed build-for-testing passed, but the final business retry again completed 12 bounded scroll operations and failed at the same chart identifier. No Production Swift changed. Stage 9 focused UI is therefore `FAIL`; table runtime assertions, sparse-Portfolio typed-unavailable flow, and Production isolation remain `NOT VERIFIED`, while Existing focused UI `7/7` and full UI `13/13` are `NOT RUN` under the ordered Gate. Unit, regression, performance, and Clean Build evidence is inherited only after exact Production/Unit source-hash verification. Provider requests remain `NOT RUN`, Twelve Data persistent writes remain `Disabled`, retention remains `BLOCKED`, and Stage 10 remains `NO-GO`.

Stage 9AAAAA adds finite, sanitized viewport-direction calibration to the Stage 9 UI test without changing Production Swift. Runtime evidence established that a positive relative delta at the top made no progress, while one negative relative delta moved the coverage anchor from inside the viewport to above it; the calibrated inverse returned coverage to the viewport. The initial focused business execution then reached all top metrics but stalled before the independent performance-summary node. The one authorized test-lifecycle repair added a single semantic-swipe fallback that could replace the calibrated wheel driver only after proving anchor progress. A fresh signed build-for-testing passed, but the final focused retry showed both the later negative delta and the single semantic swipe stalled; `analytics.chart.performance.summary` remained unexposed. Both focused executions were complete, parseable `1/1` business failures, so the business budget is exhausted. Performance/table runtime assertions, sparse-Portfolio typed-unavailable flow, and Production isolation remain `NOT VERIFIED`; Existing focused UI `7/7` and full UI `13/13` are `NOT RUN`. Provider requests remain `NOT RUN`, Twelve Data persistent writes remain `Disabled`, retention remains `BLOCKED`, and Stage 10 remains `NO-GO`.

Stage 9-UI-02 isolates Analytics viewport witnesses to real descendants of the right-side `analytics.detail.scroll` and classifies visibility with positive horizontal and vertical intersection; the fixed left-side risk-free disclosure is explicitly outside that region and is no longer a progress witness. The initial current business execution confirmed the two-dimensional calibration but exposed a bounded test-lifecycle bug: reading the identifier of an offscreen unresolved witness candidate raised an XCTest snapshot failure. The single authorized UI-test-only repair now evaluates the safe viewport relation first. A fresh final signed build-for-testing passed, and the final business retry produced real right-side progress witnesses across the performance, drawdown, monthly, and XIRR-flow regions. Nevertheless, `analytics.chart.performance.summary` remained `notExposed`; the retry stopped after seven bounded scrolls and failed its unchanged business assertion. Both focused attempts entered the business method and the authorized budget is exhausted. Existing focused UI `7/7` and full UI `13/13` are therefore `NOT RUN`; downstream table assertions, sparse-Portfolio typed-unavailable flow, and Production isolation remain `NOT VERIFIED`. Production Swift was unchanged. Provider requests remain `NOT RUN`, Twelve Data persistent writes remain `Disabled`, retention remains `BLOCKED`, and Stage 10 remains `NO-GO`.

Stage 9-AX-01 separates each native chart region into three Accessibility siblings: a visual chart group, an independent self-contained summary, and its independent native table. Performance and Drawdown identifiers, summary business text, table collections, and row semantics remain unchanged; Analytics formulas, Domain, FeatureModel, persistence, Provider routing, and the remaining product areas are byte-identical. Current-source verification passed Stage 9 focused Unit `20/20`, the complete Stage 6–8 regression `204` definitions / `237` executions, full Unit `254` definitions / `287` executions, Clean Debug arm64 Build, and fresh signed build-for-testing. Both focused business executions formally reached and passed the Performance summary/chart/table/row and Drawdown summary/chart/table/row assertions. The initial execution then failed when the bounded helper's `.fast` semantic `swipeUp` timed out while synthesizing an event during navigation toward `analytics.observed.tables`; the one authorized UI-test-only lifecycle repair changed that fallback to XCTest's default bounded swipe. A fresh final signed build-for-testing passed, but the final business retry failed at the same event-synthesis boundary with the default velocity. The business budget is exhausted, so Monthly/Annual and remaining detail tables, sparse-Portfolio typed-unavailable flow, and Production isolation remain `NOT VERIFIED`; Existing focused UI `7/7` and full UI `13/13` are `NOT RUN`. Provider requests remain `NOT RUN`, Twelve Data persistent writes remain `Disabled`, retention remains `BLOCKED`, and Stage 10 remains `NO-GO`.

Stage 9-NAV-01 replaces XCTest-generated Analytics report scrolling with a visible, keyboard- and VoiceOver-accessible native Previous/Next section navigator backed by a private eight-case section model and `ScrollViewReader`. Navigation state is view-owned, disabled without a calculated report, bounded at Overview and Calculation Evidence, and reset by calculation, Portfolio/input changes, cancellation/error, report clearing, and launch; it neither calculates, persists, nor contacts a Provider. The Stage 9 UI test now advances through the eight sections only through those buttons and retains the existing two-dimensional viewport and business assertions. Current-source Stage 9 focused Unit passed `20/20`, Stage 6–8 regression passed `204` definitions / `237` executions, full Unit passed `254` definitions / `287` executions, and both Clean Debug arm64 Build and fresh signed build-for-testing succeeded. The initial focused business execution failed after Calculate at the Overview `analytics.report.portfolio` viewport assertion. The single authorized navigation-lifecycle repair deferred the Overview `scrollTo` until the next main-actor layout opportunity and all required Unit/Build gates passed again, but the only final focused retry failed at the same assertion. Both completed result bundles are parseable `1/1` business failures; no third execution is authorized. Section transitions, remaining tables, sparse typed-unavailable behavior, and Production isolation are therefore `NOT VERIFIED`; Existing focused UI `7/7` and full UI `13/13` are `NOT RUN`. Provider requests remain `NOT RUN`, Twelve Data persistent writes remain `Disabled`, retention remains `BLOCKED`, and Stage 10 remains `NO-GO`.

Stage 9-OVERVIEW-01 separates the Overview scroll anchor from its Portfolio business node. A visible `Overview` heading owns the stable section anchor and exposes the self-contained `analytics.overview.heading` contract; the Portfolio title remains a separate `analytics.report.portfolio` node with the explicit label `Portfolio analytics report: <name>`, while coverage remains unchanged. A View-owned, non-persistent presentation identity replaces the prior `Task.yield` timing workaround and resets Overview presentation across launch, input, calculation, report, cancellation, and error lifecycle changes. The UI test now reports heading, title, and coverage count, label, and two-dimensional viewport evidence independently. Current-source Stage 9 focused Unit passed `20/20`; after retaining the signed-host `/private/tmp/AureusTests` permission failure, the same complete Stage 6–8 suites passed `204` definitions / `237` executions in the stable unsigned host; full Unit passed `254` definitions / `287` executions; Final Clean Debug arm64 Build and fresh signed build-for-testing both exited 0. The initial focused UI invocation and its one authorized infrastructure retry each timed out while enabling automation mode before the business method entered (`business executions = 0`). No business retry or third invocation is authorized, so the current focused UI remains `NOT VERIFIED`; Overview and all later runtime contracts, Sparse Portfolio, and Production isolation remain `NOT VERIFIED`, and Existing focused UI `7/7` plus full UI `13/13` are `NOT RUN`. Provider requests remain `NOT RUN`, Twelve Data persistent writes remain `Disabled`, retention remains `BLOCKED`, and Stage 10 remains `NO-GO`.

Stage 9-UI-EVIDENCE-01 changes no Production, Test, Project, Package, Migration, or Entitlement source. Exact baseline hashes preceded a fresh signed arm64 build-for-testing, which succeeded with zero errors and the four existing PortfolioView deprecation warnings. The single current-source focused UI execution entered the business method and formally passed the Synthetic initial state, Calculated Overview heading/title/coverage, all seven metrics, fixed risk-free isolation, and the Performance and Drawdown summary/chart/table/representative-row contracts. It then failed at the unchanged transition assertion for `analytics.observed.tables` after Current Section became `Observed Returns` (`AureusUITests.swift:442`). This is a `1/1` business assertion failure, not an automation bootstrap failure; no infrastructure retry or business retry was used or authorized. Monthly/Annual and all later sections, Sparse Portfolio, and Production isolation remain `NOT VERIFIED`; Existing focused UI `7/7` and full UI `13/13` are `NOT RUN`. Unit, regression, Clean Build, and Performance evidence is inherited only after exact frozen-source hash verification. Provider requests remain `NOT RUN`, Twelve Data persistent writes remain `Disabled`, retention remains `BLOCKED`, and Stage 10 remains `NO-GO`.

Stage 9-OBSERVED-01 separates the Observed Returns section anchor and business Accessibility surfaces. A visible `Observed Period Returns` heading now owns the stable section anchor and exposes the self-contained `analytics.observed.heading` label; a separate visible `analytics.observed.tables` summary reports the runtime Monthly and Annual row counts. The parent layout no longer carries either responsibility, while Monthly/Annual calculations, tables, rows, disclosure, coverage, and return semantics remain unchanged. The UI test navigates through the heading and independently verifies heading and summary count, label, and two-dimensional viewport contracts before comparing the summary's runtime counts with the real Monthly/Annual table summaries.

Required current-source verification passed Stage 9 focused Unit `20/20`; the retained signed-host `/private/tmp/AureusTests` permission failure was followed by the authorized stable unsigned Stage 6–8 result of `204` definitions / `237` executions; full Unit passed `254` definitions / `287` executions; Clean Debug arm64 Build and fresh signed build-for-testing both exited 0 with zero errors and the four existing PortfolioView warnings. The single focused business execution formally passed Overview, seven metrics, Performance, Drawdown, the new Observed heading and tables summary, Monthly/Annual, TWR Subperiods, XIRR Flows, and Capital Flows. Current Section then advanced to Calculation Evidence, but the formal `analytics.evidence` detail viewport assertion failed at `AureusUITests.swift:601`. That later-section failure is outside this round's direct repair authorization, so no source change or business retry followed. Calculation Evidence, Sparse Portfolio, stale-state isolation, and Production isolation remain `NOT VERIFIED`; Existing focused `7/7` and full UI `13/13` are `NOT RUN`. Provider requests remain `NOT RUN`, Twelve Data persistent writes remain `Disabled`, retention remains `BLOCKED`, and Stage 10 remains `NO-GO`.

Stage 9-CALC-EVIDENCE-01 separates the final report section's navigation and business Accessibility identities. The visible `Calculation Evidence` heading now owns the sole section anchor and exposes `analytics.evidence.heading` with a self-contained label; the visible no-benchmark/Provider disclosure independently owns `analytics.evidence` and its complete explicit label. The parent layout retains only containment. Calculation values, the other evidence rows, financial formulas, FeatureModel, persistence, Provider routing, and all seven earlier section contracts remain unchanged. The UI test now verifies heading and disclosure count, label, and two-dimensional viewport contracts independently and re-queries the Current/Previous/Next boundary.

Required current-source verification passed Stage 9 focused Unit `20/20`; after retaining the signed-host `/private/tmp/AureusTests` permission failure, the authorized stable unsigned Stage 6–8 result passed `204` definitions / `237` executions; full Unit passed `254` definitions / `287` executions; Clean Debug arm64 Build and the fresh signed build-for-testing both exited 0 with zero errors and the four existing PortfolioView warnings. The single Stage 9 focused UI business execution passed `1/1`, including all eight native sections, Calculation Evidence heading/disclosure and final navigation boundary, Sparse typed-unavailable and stale-state isolation, Production isolation, and the zero-Provider boundary. The conditionally executed seven-test existing focused regression then passed six tests and failed the unchanged Stage 8 Portfolio test at `AureusUITests.swift:235` while waiting for `Synthetic Second Portfolio` after navigation; it was a real business assertion failure, so it was not fixed or rerun and Full `AureusUITests` is `NOT RUN`. Provider requests remain `NOT RUN`, Twelve Data persistent writes remain `Disabled`, retention remains `BLOCKED`, and Stage 10 remains `NO-GO`.

Stage 9-PORTFOLIO-ORDER-01 adds a visible, self-contained `portfolio.order.status` node whose label reports the selected Portfolio's actual position in the current collection. The Stage 8 Portfolio UI lifecycle now waits for the real order states `3 of 3`, `2 of 3`, and `1 of 3`, clicks Move Up exactly once per transition, and rebuilds through Dashboard only after the selected Portfolio reaches position 1. Dashboard and Portfolio destinations are awaited explicitly and all post-navigation elements are re-queried; create, UUID rows, delete/reload, Production isolation, and Provider isolation assertions remain intact.

Current-source Stage 9 focused Unit passed `20/20`; the retained signed-host App Sandbox failure was followed by the authorized stable unsigned Stage 6–8 result of `204` definitions / `237` executions; full Unit passed `254` definitions / `287` executions; Clean Debug arm64 Build and two fresh signed build-for-testing invocations exited 0 with zero errors and four existing warnings. Gate A's initial targeted Portfolio UI invocation and the global single infrastructure retry both failed before any business method entered because the runner timed out while enabling automation mode. Both complete bundles contain only a runner system-failure node and `0` business definitions/executions. Therefore the new order-status runtime states remain `NOT VERIFIED`; Stage 9 focused UI, Existing focused `7/7`, and full UI `13/13` are `NOT RUN` under the ordered Gate. Provider requests remain `NOT RUN`, Twelve Data persistent writes remain `Disabled`, retention remains `BLOCKED`, and Stage 10 remains `NO-GO`.

Stage 9-UI-RUNTIME-EVIDENCE-01 changes no Production, Test, Project, Package, Migration, Entitlement, Target, Scheme, or Synthetic fixture. Exact source-hash verification preceded one fresh signed arm64 build-for-testing; App, Runner, UI Test, and xctestrun were then frozen and retained identical SHA-256 values across every invocation. On that same product, the targeted Stage 8 Portfolio test passed `1/1` and formally observed order states `3 of 3` → `2 of 3` → `1 of 3`, the disabled top boundary, Dashboard/Portfolio reconstruction, stable UUID rows, confirmed deletion, second reload, Production isolation, and Provider isolation. The Stage 9 focused test passed `1/1`; the ordered Existing focused regression passed `7/7`; and the full `AureusUITests` suite passed `13/13`, all with zero failures and zero skips. Every generated result bundle contains `Info.plist` and has a final successful build or test-results parse. No infrastructure or business retry was used. This is UI evidence completion candidate material only: Reviewer Gate authority is unchanged, Provider requests remain `NOT RUN`, Twelve Data persistent writes remain `Disabled`, retention remains `BLOCKED`, and Stage 10 remains `NO-GO`.

Twelve Data remains the selected Primary Market Data Provider under **Personal Local Mode**: Aureus runs only on one user's Mac for personal/internal, non-commercial use; it is not a hosted service and does not redistribute or commercially display Provider data. Open source applies to program source, not Provider data, credentials, or user financial data. Basic Free remains the usable US-focused entry path, while every endpoint and MIC remains governed by the actual Plan and exchange entitlement.

Twelve Data Production data is session-only in V1. Search, Quote, OHLCV, and entitled Corporate Actions route through one dependency-injected, actor-owned 64 MiB transient memory store with typed TTL, deterministic LRU, checked logical byte accounting, lifecycle clearing, and no serialization or cross-launch recovery. Stage 6MA makes exact expiry stale, clears the complete Provider session before returning confirmed credential/entitlement loss, generation-checks every fresh, stale-fallback, stored, and oversize return, routes Settings Clear through the service lifecycle boundary, and requires complete Split plus Dividend state. Production persistent Twelve Data writes are **Disabled by product policy**; query paths neither read nor write Twelve Data disk rows, and startup performs a Provider-scoped legacy-row purge. Retention rights remain `BLOCKED`, but this deferred disk-cache question is no longer proposed as a permanent Stage 7 implementation blocker. Minimal user-authored symbol/MIC identifiers and UI preferences may persist without Provider descriptions or values.

Prompt 6NBA received Reviewer Gate `PASS` after classifying the previous one-attempt Search failure as `SEARCH_INVALID_PAYLOAD`. Stage 6NBB permanently repairs `/symbol_search` at the row boundary: it keeps strict envelope validation, isolates malformed and out-of-scope rows, preserves Provider relevance order and normalized raw MIC, performs stable first-result de-duplication, and does not expand the V1 market-currency set. Its one live Search responded successfully after exactly one transport attempt, but the old harness accepted only `AAPL/XNAS` and did not report the actual raw MIC; that evidence cannot be retrospectively promoted to an exact US identity or Historical entitlement. Stage 6NBC corrects the criterion to exact symbol `AAPL`, currency `USD`, and raw MIC in `XNAS`, `XNYS`, `XASE`, `ARCX`, `BATS`, `XNCM`, `XNGS`, or `XNMS`, while preserving the selected raw MIC and aggregating capability only as `US`. First-party Twelve Data pages identify AAPL with `XNGS` as catalog/documentation evidence. The single bounded Production re-observation then selected `AAPL/USD/XNGS` in Provider relevance order and returned a nonempty typed `1day`/`.all`/output-size-5 Historical page after exactly two local transport attempts total, with raw MIC `XNGS` retained and observed only under the aggregate `US` capability. This verifies the Search identity and that precise Historical operation only for the current credential at the observed instant; it does not prove that the Actual Plan is Basic or extend to Quote, Corporate Actions, freshness, other MICs, or `XHKG`/`XSHG`/`XSHE`/`XJPX`, which remain `NOT VERIFIED`.

The 2026-08-17 Terms refresh records §12.5 as immediate cessation of access with a deletion obligation and §16.2 as the operational deadline to delete within 30 days after termination or expiration. Aureus keeps immediate Provider-scoped purge as a stricter internal policy, not as a claimed Provider deadline. `/api_usage` remains `NOT VERIFIED`. No real Provider request was performed for Stage 7 implementation or automated verification.

Stage 7 preserves the accepted live evidence boundary: only the previously observed `AAPL/USD/XNGS` Search identity and precise `1day`/`.all` Historical operation are recorded as verified at that observation instant. Actual Plan, Quote, Corporate Actions, actual freshness, other symbols/MICs, and all international markets remain `NOT VERIFIED`. Twelve Data persistent writes remain **Disabled** and retention rights remain **BLOCKED**. Synthetic Demo UI and tests are conspicuously identified and never count as live acceptance.

Stage 7A freezes chart panes as candlestick/main overlays `0`, volume `1`, RSI `2`, and MACD `3`; the local renderer returns only a sanitized series/type/pane/point-count summary for integration evidence. Range requests now use Gregorian UTC civil dates (`1D` latest returned daily bar, calendar `1W/1M/3M/YTD/1Y/5Y`, and bounded `MAX`) and presentation filters inclusively. Zoom/pan visible-range callbacks use typed dates and drive the native summary and accessible table. Theme updates, bridge byte/string/date limits, Watchlist reorder controls, and range-scoped multi-instrument heatmap values remain local and do not trigger or imply Provider capability.

Stage 7AA preserved the four earlier Stage 7A accessibility failures as historical evidence, then tested the final source without a pre-test source change. The independent `markets.chart.status` and `markets.chart.visible-range` nodes passed the first bounded Stage 7 focused UI run (`1/1`), so no AX-only repair or focused retry occurred. The single authorized full UI run passed `11/11`; Stage 7 focused Unit/Integration passed `26/26`, the stable unsigned full Unit suite passed `210/210` definitions (`243` executions), and final clean build plus build-for-testing passed. These local and synthetic results close the implementation evidence requested by Prompt 7AA but do not decide the Reviewer Gate or expand live Provider acceptance.

Stage 8 appends `permanent_v6_portfolio` without changing the v1–v5 migration blocks. Active Portfolio tables are separate from legacy foundation placeholders, use foreign keys and INTEGER authoritative values, and preserve Wealth/Ledger deletion boundaries. Wealth manual marks and saved FX provenance are the only persistent valuation inputs. Benchmark values are loaded only after an explicit user action, remain in the transient market session, compare exact overlapping civil dates at base 100, and are never stored in Portfolio tables or preferences. Stage 9 analytics do not add a migration or persistence path; brokerage sync, live pricing, investment advice, tax analysis, and Stage 10 functionality remain outside scope.

- [V1 Scope — Frozen](docs/V1_SCOPE.md)
- [V1 Architecture & Technology — Frozen](docs/V1_ARCHITECTURE.md)
- [V1 Research Evidence](docs/V1_RESEARCH_EVIDENCE.md)
- [Stage 6 Market Data Acceptance Evidence](docs/STAGE6_MARKET_DATA_ACCEPTANCE.md)
- [Stage 6 Twelve Data Retention Decision](docs/STAGE6_TWELVE_DATA_RETENTION_DECISION.md)
- [Stage 7 Markets Terminal Acceptance](docs/STAGE7_MARKETS_TERMINAL_ACCEPTANCE.md)
- [Stage 8 Portfolio Acceptance](docs/STAGE8_PORTFOLIO_ACCEPTANCE.md)
- [Stage 9 Portfolio Analytics Acceptance](docs/STAGE9_ANALYTICS_ACCEPTANCE.md)
- [Stage 10 Goals Acceptance](docs/STAGE10_GOALS_ACCEPTANCE.md)
- [Third-Party Notices](THIRD_PARTY_NOTICES.md)

## Build and Test

Development agents follow [AGENTS.md](AGENTS.md). The commands below are reference examples, not instructions to execute while reading this file. Choose checks according to the current authorization and change risk; documentation-only changes normally need document checks. An explicit Stage Gate still requires its prescribed suites, order, product identity, and retry limits. Keep macOS UI runs serial on a shared desktop, and use task-specific temporary evidence paths rather than reusing the example paths across concurrent work.

Resolve the single external dependency, GRDB 7.11.1:

```sh
xcodebuild -resolvePackageDependencies -project Aureus.xcodeproj -scheme Aureus
```

Build the arm64 Debug app and all test products in isolated DerivedData:

```sh
xcodebuild -project Aureus.xcodeproj -scheme Aureus -configuration Debug -destination 'platform=macOS,arch=arm64' -derivedDataPath /private/tmp/Aureus-Stage9-DerivedData clean build
xcodebuild -project Aureus.xcodeproj -scheme Aureus -configuration Debug -destination 'platform=macOS,arch=arm64' -derivedDataPath /private/tmp/Aureus-Stage9-DerivedData build-for-testing
```

When a complete regression is authorized, run the Unit/Integration suite and then the UI suite as required by that Gate. The UI test runner uses only local ad-hoc signing and does not require a Development Team:

```sh
xcodebuild test -project Aureus.xcodeproj -scheme Aureus -configuration Debug -destination 'platform=macOS,arch=arm64' -derivedDataPath /private/tmp/Aureus-Stage9-Unit -only-testing:AureusTests CODE_SIGNING_ALLOWED=NO
xcodebuild test -project Aureus.xcodeproj -scheme Aureus -configuration Debug -destination 'platform=macOS,arch=arm64' -derivedDataPath /private/tmp/Aureus-Stage9-UI -only-testing:AureusUITests
```

The canonical product design source for later stages is [Aureus_Wealth_Terminal_项目设计汇总.md](Aureus_Wealth_Terminal_项目设计汇总.md).

The specific open-source license has not yet been frozen. All Git and GitHub operations are managed manually by the user.

## External Backup Restore Foundation round

Prompt 11-EXTERNAL-BACKUP-RESTORE-FOUNDATION-01 adds a no-UI, explicitly invoked External Backup Restore boundary. It accepts only a complete two-file Aureus generation, performs standalone authoritative manifest, digest, byte-count, read-only SQLite, foreign-key, schema, type, name, and symlink validation, rejects overlap with protected internal locations, stages only the validated database beside the live Permanent Store, and then reuses the accepted safety-Backup, atomic replacement, forward-migration, post-validation, rollback, and `recoveryRequired` core. Internal Restore remains restricted to a configured Backup-root direct child. External source artifacts are never imported into internal inventory, retained, renamed, or rewritten; the only eligible new internal generation is the pre-Restore safety Backup.

The current evidence root is `/private/tmp/Aureus-Stage11-EXTERNAL-BACKUP-RESTORE-FOUNDATION-01-1l01JA`. The post-repair Focused Unit result is complete and canonically parseable but remains `FAIL`: `78` definitions / `107` dynamic executions, `106/1/0`, shell exit `65`. All Restore, rollback, schema 1...5, isolation, privacy, and 10,000-row External Restore cases reached in that run passed; the sole failure was the schema-0 fixture's attempt to set `schema_metadata.version = 0`, which the existing database `CHECK (version >= 1)` rejected before the External Restore API executed. Because the one authorized direct repair had already corrected `/private/tmp` URL canonicalization, no further repair or retry was performed. Affected Regression, Full Unit, Release Performance, Clean Build, and signed BFT are `NOT RUN`; UI Tests are `NOT RUN — NOT AUTHORIZED IN EXTERNAL BACKUP RESTORE FOUNDATION ROUND`.

Settings External Restore UI, file panels, security-scoped runtime, bookmark/history persistence, automatic or scheduled Restore, external retention, cloud Restore, and Stages 12–14 remain `NOT IMPLEMENTED / NOT AUTHORIZED` or `NO-GO` as applicable. Provider requests remain `NOT RUN`; Twelve Data persistent writes remain `Disabled`; Provider retention rights remain `BLOCKED`.

## External Backup Restore validation closure round

Prompt 11-EXTERNAL-BACKUP-RESTORE-VALIDATION-CLOSURE-01 fixes the two bounded validation issues from the preceding Foundation round. The schema-0 test now submits an otherwise valid schema-6 External generation whose untrusted manifest alone declares schema `0`, invokes the formal External Restore API, and verifies typed `invalidExternalCandidate(.malformedManifest)` rejection before staging, safety Backup, replacement, or Store mutation. The distinct schema-7 path re-signs a database and manifest that both declare schema `7`, invokes the same API, and verifies typed `.schemaMismatch` rejection with the same zero-side-effect boundary.

Standalone External generation validation now validates only the explicitly selected generation URL and its two children. It no longer derives, lists, validates, or otherwise requires access to the selected directory's parent. A single authoritative generation-content validator still enforces the strict name, ordinary non-symlink directory and files, exact two-file artifact, manifest format and canonical fields, streaming digest, byte count, query-only SQLite quick-check, foreign keys, and schema agreement. Internal `validateGeneration(_:in:)` still performs configured-root and direct-child checks first; External Export committed and staging artifacts likewise remain destination-root direct children.

Current evidence root: `/private/tmp/Aureus-Stage11-EXTERNAL-BACKUP-RESTORE-VALIDATION-CLOSURE-01-skIX9p`.

| Verification | Result | Evidence |
|---|---|---|
| Focused Unit | `PASS` | Exact suites `PermanentExternalRestoreTests`, `PermanentRestoreTests`, and `PermanentBackupTests`; shell exit `0`; `79` definitions / `107` dynamic executions; `107/0/0`; complete `FocusedUnit.xcresult`; `Info.plist` present; initial sandbox parser exits `64/64`, same-bundle standard-permission parser exits `0/0` |
| Affected Regression | `PASS` | Seven exact suites; shell exit `0`; `161` definitions / `195` dynamic executions; `195/0/0`; complete `AffectedRegression.xcresult`; `Info.plist` present; parsers `0/0` |
| Full Unit | `PASS` | Exact selector `AureusTests`; shell exit `0`; `454` definitions / `521` dynamic executions; `521/0/0`; complete `FullAureusTests.xcresult`; `Info.plist` present; parsers `0/0` |
| Release External Restore | `PASS` | Exact suite `PermanentExternalRestoreTests`; shell exit `0`; `25` definitions / `49` dynamic executions; `49/0/0`; complete `ReleaseExternalRestorePerformance.xcresult`; `Info.plist` present; parsers `0/0` |
| Release workload | `PASS` | `STAGE11_EXTERNAL_RESTORE_PERF rows=10000 validate_safety_restore_ms=43 migration_applied=0 provider_requests=0 cache_reads=0 credential_reads=0` |
| Clean Debug arm64 Build | `PASS` | Shell exit `0`; status `succeeded`; errors `0`; four existing `PortfolioView` warnings and no new warnings; complete `CleanDebugBuild.xcresult`; `Info.plist` present; parser exit `0` |
| Fresh signed arm64 BFT | `PASS` | Shell exit `0`; `TEST BUILD SUCCEEDED`; errors `0`; four existing warnings and no new warnings; complete `BuildForTesting.xcresult`; `Info.plist` present; parser exit `0`; App/Runner strict codesign exits `0` |
| UI Tests | `NOT RUN — NOT AUTHORIZED IN EXTERNAL BACKUP RESTORE VALIDATION CLOSURE ROUND` | Historical `17/17` is not represented as current-source UI evidence |

Settings External Restore UI remains `NOT IMPLEMENTED / NOT AUTHORIZED`; security-scoped runtime is `NOT RUN`. No destination, source URL, bookmark, or history is persisted. Provider requests remain `NOT RUN`, Twelve Data persistent writes remain `Disabled`, Provider retention rights remain `BLOCKED`, and Stages 12–14 remain `NO-GO`. This candidate does not decide the Stage 11 Reviewer Gate.

## Settings External Backup Restore UI round

Prompt 11-SETTINGS-EXTERNAL-BACKUP-RESTORE-UI-01 connects the accepted External Restore Foundation to the existing Settings data-lifecycle surface. Production composition reuses the same `WealthStore`, injects protected RuntimePaths, and exposes a finite, sanitized External Restore client/result boundary. The UI uses one shared SwiftUI `.fileImporter(.folder)` for External Export and External Restore, an operation-scoped security-scoped lease, and an app-owned confirmation sheet. It stores no URL, filename, bookmark, or history and does not add raw SQLite import, automatic Restore, scheduling, cloud Restore, or external retention.

Current evidence root: `/private/tmp/Aureus-Stage11-SETTINGS-EXTERNAL-BACKUP-RESTORE-UI-01-A9c0OF`.

| Verification | Result | Evidence |
|---|---|---|
| Focused Unit | `PASS` | Final-source exact Settings/lifecycle/Foundation selectors; shell exit `0`; `134` definitions / `168` dynamic executions; `134/0/0`; complete `FocusedUnit-Final.xcresult`; `Info.plist` present; summary/tests parser exits `0/0` |
| Affected Regression | `PASS` | Final-source exact affected selectors; shell exit `0`; `167` definitions / `205` dynamic executions; `167/0/0`; complete `AffectedRegression-Final.xcresult`; `Info.plist` present; parsers `0/0` |
| Full Unit | `PASS` | Exact selector `AureusTests`; shell exit `0`; `460` definitions / `531` dynamic executions; `460/0/0`; complete `FullAureusTests-Final.xcresult`; `Info.plist` present; parsers `0/0` |
| External Restore performance | `NOT RUN — INHERITED AFTER EXACT FOUNDATION SOURCE-HASH VERIFICATION` | Accepted Release suite `25/49 PASS`; real 10,000-row workload `43 ms` |
| External Export performance | `NOT RUN — INHERITED AFTER EXACT FOUNDATION SOURCE-HASH VERIFICATION` | Accepted Release suite `29/31 PASS`; real 10,000-row workload `26 ms` |
| Clean Debug arm64 Build | `PASS` | Final source; shell exit `0`; canonical status `succeeded`; errors `0`; four pre-existing `PortfolioView` warnings and no new warnings; complete `CleanDebugBuild-Final.xcresult`; `Info.plist` present; parser exit `0` |
| Fresh signed arm64 BFT | `PASS` | Final source; shell exit `0`; `TEST BUILD SUCCEEDED`; errors `0`; four existing warnings and no new warnings; complete `BuildForTesting-Final.xcresult`; `Info.plist` present; parser exit `0`; App/Runner strict codesign exits `0` |
| Targeted External Restore UI initial | `FAIL` | One complete business execution; `0/1/0`; the External Export folder panel did not appear because two sibling folder importers competed; the bundle is retained and was not counted as PASS |
| Targeted External Restore UI final | `FAIL` | Exact selector `testStage11SettingsExternalBackupRestoreFromUserSelectedGenerationAndIsolation`; one complete business execution; canonical `0/1/0`; method duration `45.083 s`; complete `TargetedExternalRestoreUI-Final.xcresult`; `Info.plist` present; summary/tests parser exits `0/0`; failure `AureusUITests.swift:3042`, public Choose control unavailable after Go To Folder |
| Existing focused `12/12` | `NOT RUN` | Gate H did not pass; ordered prerequisite not met |
| Full `AureusUITests` `18/18` | `NOT RUN` | Gate I was not run/passed |

The single authorized direct repair was used before the final C–G rerun to consolidate the two folder importers into one shared operation-dispatched importer. The second Targeted execution is a complete business failure at the External Export panel-control lifecycle, before External Restore source selection, security-scoped Restore access, confirmation, safety generation, restored Goals, reconstruction, or Production-isolation tail. No second repair, third Targeted execution, infrastructure retry, or incomplete-result re-observation was performed. Provider requests are `NOT RUN`; Twelve Data operations, Frankfurter live operations, transport attempts, Credential reads, Keychain metadata reads, and Market Cache reads/mutations remain `0`; Twelve Data persistent writes remain `Disabled`; Provider retention rights remain `BLOCKED`; Stages 12–14 remain `NO-GO`.

**Stage 11-SETTINGS-EXTERNAL-BACKUP-RESTORE-UI-01 PARTIAL — Awaiting Reviewer Gate**

## External Restore directory-panel closure round

本轮 evidence root：`/private/tmp/Aureus-Stage11-EXTERNAL-RESTORE-DIRECTORY-PANEL-CLOSURE-01-bEN0A5`。Product、Model、Foundation、Unit、Project、Package、Migration 和 Entitlements 的 Hash 均保持冻结值；只有 `chooseDirectory` 改为在 Go To sheet 消失、当前 native panel 仍存在后执行一次 `.enter`。`chooseFile`、`saveFileUsingDefaultFilename` 和全部业务断言保持不变。

| Gate | 实际结果 |
|---|---|
| Focused / Affected / Full Unit | `NOT RUN — INHERITED AFTER EXACT SOURCE-HASH VERIFICATION`；accepted definitions/dynamic executions 分别 `134/168`、`167/205`、`460/531 PASS` |
| External Restore / Export Performance | `NOT RUN — INHERITED AFTER EXACT FOUNDATION SOURCE-HASH VERIFICATION`；accepted `25/49 PASS, 43 ms` 与 `29/31 PASS, 26 ms` |
| Clean Debug arm64 Build | `PASS`；direct shell exit `0`；canonical `succeeded`；errors `0`，四项既存 Portfolio interpolation warnings；`33.989 s`；完整 `CleanDebugBuild.xcresult`，`Info.plist` 存在，build parser `0` |
| Fresh signed Debug arm64 BFT | `PASS`；direct shell exit `0`；`TEST BUILD SUCCEEDED`；errors `0`，四项既存 warnings；`37.401 s`；完整 `BuildForTesting.xcresult`，`Info.plist` 存在，build parser `0`，App/Runner strict codesign `0/0` |
| Targeted External Restore UI | `PASS`；direct shell exit `0`；definitions/executions `1/1`；business executions `1`；`1/0/0`；method `228.097 s`，result interval `240.645 s`；完整 `TargetedExternalRestoreUI.xcresult`，`Info.plist` 存在，summary/tests parsers `0/0` |
| Existing focused | `FAIL`；definitions/executions `12/12`，canonical `0/12/0`；Ledger 已进入业务操作后发生 application connection loss，后续 11 个方法在公共 pre-launch helper 中失败；完整 `ExistingFocusedRegression.xcresult`，`Info.plist` 存在，summary/tests parsers `0/0`；`214.574 s`；command-session interruption 后原 session 不可恢复，direct shell 数字退出码为 `NOT VERIFIED` |
| Full UI | `NOT RUN`；Existing focused 未取得 `12/12 PASS`，未执行 expected `18/18` |

Targeted 已证明公开目录默认动作修复有效；其独立 PASS 不替代失败的聚合 Gate。Existing focused 的首个 failure source 为 `AureusUITests.swift:2444`，查询 `ledger.filter.tag` 时与应用连接丢失；后续 source 为 `AureusUITests.swift:3525`。连接丢失的底层原因为 `NOT VERIFIED`，不得推断为 Restore/Foundation 数据失败或将完整失败改写为 incomplete。两次 UI invocation 使用同一个 frozen signed arm64 product，前后四项 Hash 均一致。

执行记录还存在一项前置限制：45 项 Mandatory Read 曾按序经文件流读取到 EOF，但命令将内容重定向到 `/dev/null`，该记录不足以证明逐项内容审阅；AC-01 为 `NOT VERIFIED`。历史 bundle 均只读保留。Provider requests 为 `NOT RUN`；Twelve Data/Frankfurter/transport/Credential/Keychain/Market Cache live operations 为 `0`；persistent writes 为 `Disabled`，Provider retention rights 为 `BLOCKED`；external retention、scheduling、cloud Restore 为 `NOT IMPLEMENTED / NOT AUTHORIZED`；Stages 12–14 保持 `NO-GO`。

**Stage 11-EXTERNAL-RESTORE-DIRECTORY-PANEL-CLOSURE-01 PARTIAL — Awaiting Reviewer Gate**
